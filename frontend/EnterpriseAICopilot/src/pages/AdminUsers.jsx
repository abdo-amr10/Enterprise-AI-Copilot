import {useState} from 'react';import AdminSidebar from '../components/AdminSidebar';import AdminTopBar from '../components/AdminTopBar';import '../styles/admin.css';
export default function AdminUsers(){const [state,setState]=useState('list');return <main className="admin-shell"><AdminSidebar active="users"/><section className="admin-main"><AdminTopBar title="Users" description="Create and manage Normal User access across the enterprise."/>{state==='list'?<><div className="admin-page-actions"><span>248 active users</span><button className="primary" onClick={()=>setState('form')}>Add user</button></div><div className="admin-table"><div className="table-row table-head"><span>Name</span><span>Role</span><span>Branch</span><span>Status</span></div>{[['Ahmed Hossam','Normal User','Cairo HQ'],['Mariam Adel','Normal User','Alexandria'],['Omar Khaled','Admin','Cairo HQ']].map(r=><div className="table-row"><span><b>{r[0]}</b><small>{r[0].toLowerCase().replace(' ','')}@enterprise.com</small></span><span>{r[1]}</span><span>{r[2]}</span><span className="admin-badge">Active</span></div>)}</div></>:<article className="admin-card admin-form"><small>NEW ACCOUNT</small><h3>Register a user</h3><p>Accounts are created by an authorized Admin. The user role determines their available workspace.</p><div className="admin-fields"><input placeholder="First name"/><input placeholder="Last name"/><input placeholder="Work email"/><select defaultValue=""><option value="" disabled>Select role</option><option>Normal User</option><option>Admin</option></select><select defaultValue=""><option value="" disabled>Select branch</option><option>Cairo HQ</option><option>Alexandria</option></select></div><div className="admin-actions"><button onClick={()=>setState('list')}>Cancel</button><button className="primary" onClick={()=>setState('success')}>Create user</button></div>{state==='success'&&<p className="admin-success">User registered successfully.</p>}</article>}</section></main>}
